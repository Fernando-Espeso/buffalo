<div class="contact-section">
  <div class="info">
    {{  section.settings.info
      | replace: '</p><p>', '<br>'
      | replace: '<p>', ''
      | replace: '</p>', ''
      | raw
   }}
  </div>

  <div class="info">
    {{ section.settings.info-2
      | replace: '</p><p>', '<br>'
      | replace: '<p>', ''
      | replace: '</p>', ''
      | raw
   }}
  </div>
</div>

{% schema %}
{
  "name": "Contact Section",
  "settings": [
    {
      "type": "richtext",
      "id": "info",
      "label": "Info Content"
    },
    {
      "type": "richtext",
      "id": "info-2",
      "label": "Info Content"
    }
  ]
}
{% endschema %}
